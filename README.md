# CampusForge
An Academic skill and collaboration platform

It is an platform

## Announcement email notifications

Club announcements email approved club members; event announcements email approved
event registrants. Copy `backend/.env.example` to `backend/.env` and provide
your SMTP provider credentials. When SMTP is not configured, announcements are
still published and the UI clearly says that email delivery is not configured.
